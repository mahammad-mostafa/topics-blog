<h2>New Comment</h2>

<%= form_with(model: [@post, Comment.new]) do |form| %>
  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>
  <div class="actions">
    <%= form.submit "Add Comment" %>
  </div>
<% end %>

<%= link_to 'Back', post_path(@post) %>
